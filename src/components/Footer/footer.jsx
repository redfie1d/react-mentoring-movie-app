import './footer.css';
import Logo from 'components/Logo/logo';

const Footer = () => {
  return (
    <footer className="footerContainer">
      <Logo />
    </footer>
  );
};

export default Footer;
